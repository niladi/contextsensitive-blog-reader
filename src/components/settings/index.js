import Vue from 'vue'

Vue.component('settings-default', require('./Default').default)
Vue.component('settings-noise', require('./Noise').default)
Vue.component('settings-speed', require('./Speed').default)
